- name: configure DB server
  hosts: db 
  become: yes
  vars: 
    login_host: db.daws78s.shop
  tasks: 
  - name: Install MySql server
    ansible.builtin.dnf:
      name: mysql-server
      state: latest

  - name: Start Enable MySql server 
    ansible.builtin.service:
      name: mysqld
      state: started 
      enabled: yes 
  #check password is alredy setup or not
  - name: check DB connection 
    community.mysql.mysql_info:
      login_user: root 
      login_password: ExpenseApp@1 
      login_host: "{{ login_host }}" 
      filter: version         